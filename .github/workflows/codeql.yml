name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

jobs:
  analyze:
    uses: tskovlund/.github/.github/workflows/codeql.yml@7d7b797de0edefa4922c24275e3528da0e15861b # main
    with:
      language: javascript-typescript
    permissions:
      security-events: write
