{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.16.0/.schema/devbox.schema.json",
  "packages": ["nodejs@22", "pnpm@latest"],
  "shell": {
    "init_hook": ["echo 'Welcome to devbox!' > /dev/null"],
    "scripts": {
      "dev": "pnpm dev",
      "build": "pnpm build",
      "preview": "pnpm preview"
    }
  }
}
